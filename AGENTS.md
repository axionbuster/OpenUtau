# Repository working agreements

After each coherent, verified increment committed directly on `master`, push it
promptly to `origin/master`. This is standing authorization for this OpenUtau
repository, so no additional permission is needed for those routine pushes.

Use normal non-force pushes. Do not force-push, rewrite published history, or
perform destructive remote operations unless the user explicitly requests it.
